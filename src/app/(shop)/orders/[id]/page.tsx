import { titleFond } from "@/config/fonds";

function page() {
  return (
    <div>
      <h1 className={`${titleFond.className} font-bold`}>Order ## abc </h1>
    </div>
  );
}

export default page;
